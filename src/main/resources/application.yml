spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/car_rental
    username: postgres
    password: password
  sql:
    init:
      mode: always
server:
  port: 8081
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/demo